{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Limits Configuration v1",
  "type": "object",
  "properties": {
    "house_price_warn_per_day": { "type": "integer", "minimum": 0 },
    "house_price_hard_stop_per_day": { "type": "integer", "minimum": 0 },
    "house_orders_per_sec": { "type": "integer", "minimum": 0 },
    "house_positions_pending_limit": { "type": "integer", "minimum": 0 }
  },
  "required": [
    "house_price_warn_per_day",
    "house_price_hard_stop_per_day",
    "house_orders_per_sec",
    "house_positions_pending_limit"
  ]
}
